# REST API 개념 및 설계 원칙

### 📝 목차
1. [REST란 무엇인가?](#rest란-무엇인가)
2. [API와 REST API, RESTful](#api와-rest-api-restful)
3. [REST API의 장단점](#rest-api의-장단점)
4. [REST의 6가지 설계 원칙](#rest의-6가지-설계-원칙)

---
<br>

## REST란 무엇인가?
**REST**(Representational State Transfer)는 자원의 표현을 통해 상태를 주고받는 네트워크 아키텍처 스타일입니다. 
> **자원**: 소프트웨어가 관리하는 모든 것<br>**자원의 표현**: 자원을 나타내기 위한 이름

HTTP URI를 통해 제어할 자원을 명시하고 HTTP Method(GET, POST, PUT, DELETE)를 통해 CRUD 연산을 적용하는 것을 의미합니다.

<br><br>

## API와 REST API, RESTful
- **API**(Application Programming Interface): 프로그램 간 정보를 주고받을 수 있는 창구
- **REST API**: REST 아키텍처 원칙을 기반으로 구현된 API
- **RESTful**: REST 원칙을 충실히 지키는 시스템

<br><br>

## REST API의 장단점
### 장점
- 역할 분리: 서버와 클라이언트의 역할을 명확하게 분리하여 독립적인 개발이 가능합니다.
- 범용성: HTTP 프로토콜을 따르는 모든 플랫폼에서 사용할 수 있습니다.
- 테스트 용이성: CURL, Postman 같은 도구로 간단하게 테스트 할 수 있습니다.
- 자기 서술적: JSON 구조가 바뀌어도 필요한 값만 골라 쓸 수 있어 하위 호환성 보장에 유리합니다.

### 단점
- 통신 모델의 한계: 오직 요청과 응답 스타일의 통신만 지원합니다.
- 표현의 제약: HTTP 메서드만으로는 복잡한 비즈니스 로직을 모두 표현하기 어려운 경우가 발생합니다.
- 비효율적인 데이터 전송
  - 필요한 데이터를 얻으려면 여러 번 요청해야 하는 경우 발생 (Under-fetching)
  - 필요 없는 데이터까지 받아오는 경우 발생 (Over-fetching)
- 오버헤드: JSON 같은 텍스트 포맷은 메시지 길이가 길어질 수 있으며, 파싱 과정에서 성능 오버헤드가 발생할 수 있습니다.

<br><br>

## REST의 6가지 설계 원칙
> RESTful한 API를 설계하기 위해 준수해야 하는 가이드라인입니다.
1. **Client-Server 구조**: 자원을 가진 서버와 요청을 하는 클라이언트가 서로 의존하지 않고 독립적이어야 합니다.
2. **Stateless(무상태성)**: 서버는 클라이언트의 이전 상태를 별도로 저장하지 않으며, 각 요청은 독립적으로 처리됩니다.
3. **Cacheable(캐시 가능성)**: HTTP 캐싱 기능을 활용하여 서버의 부하를 줄이고 응답 속도를 높일 수 있습니다.
4. **Layered System(계층화)**: 클라이언트는 서버의 중간 매체(프록시, 게이트웨이 등) 유무를 알 수 없습니다.
5. **Code-On-Demand(optional)**: 서버가 클라이언트로 스크립트를 보내 실행할 수 있으며 반드시 충족할 필요는 없습니다.
6. **Uniform Interface(일관된 인터페이스)**: URI로 지정한 리소스에 대한 조작이 통일되고 한정적인 인터페이스로 수행되어야 합니다.
