# HTTP 통신의 기본 개념과 동작 원리

### 📝 목차
1. [HTTP란 무엇인가?](#http란-무엇인가)
2. [HTTP의 핵심 특징](#http의-핵심-특징)
3. [HTTP 메시지 구조](#http-메시지-구조)
4. [HTTP 메서드와 상태 코드](#http-메서드와-상태-코드)
5. [HTTP vs HTTPS](#http-vs-https)

---
<br>

## HTTP란 무엇인가?
**HTTP**(HyperText Transfer Protocol)는 웹 상에서 클라이언트와 서버가 데이터를 주고받기 위한 통신 규약(프로토콜)입니다.<br>
텍스트, 이미지, 비디오 등 모든 형태의 자원을 전송할 때 사용되며 오늘날 거의 모든 웹 통신은 이 규칙을 기반으로 이루어집니다.

<br><br>

## HTTP의 핵심 특징
- **Stateless(무상태성)**: 서버는 클라이언트의 이전 상태를 기억하지 않습니다. 각 요청은 독립적이며 서버는 요청에 필요한 정보만 확인하여 응답합니다.
- **Connectionless(비연결성)**: 요청에 대한 응답을 마치면 연결을 즉시 끊습니다.
  - 장점: 서버 자원을 효율적으로 관리할 수 있다.
  - 단점: 매번 연결을 새로 맺는 비용이 발생한다.

<br><br>

## HTTP 메시지 구조
HTTP 통신은 요청(Request)과 응답(Response)의 쌍으로 이루어집니다.
<br><br>
메시지는 크게 세 부분으로 구성됩니다.

**1. Start Line**
> Request: Method, Target, Version 등이 포함됩니다.<br>Response: Version, Status Code, Status Text가 포함됩니다.

<br>

**2. Header**
> 메시지 전송에 필요한 부가 정보(메타데이터)가 담깁니다.<br>예를 들면 Content-Type, User-Agent, Cookie 등이 여기에 포함됩니다.

<br>

**3. Body**
> 실제 전송할 데이터가 담기는 영역입니다.<br>REST API에서는 주로 JSON 포맷의 데이터를 Body에 담아 전송합니다.

<br><br>

## HTTP 메서드와 상태 코드
### HTTP 메서드
- **GET**: 자원 조회
- **POST**: 새로운 자원 생성
- **PUT**: 자원의 전체 수정
- **DELETE**: 자원 삭제

<br>

### 상태 코드
- 2xx(Success): 요청이 성공적으로 처리됨 (ex. 200 OK)
- 3xx(Redirection): 요청 완료를 위해 추가 동작이 필요함
- 4xx(Client Error): 클라이언트의 요청에 오류가 있음 (ex. 404 Not Found)
- 5xx(Server Error): 서버 측의 문제로 요청 처리에 실패함 (ex. 500 Internal Server Error)

<br><br>

## HTTP vs HTTPS
HTTP는 데이터를 암호화하지 않은 평문 상태로 전송하기 때문에, 통신 과정에서 제3자가 정보를 가로채거나 조회할 위험이 큽니다.
<br>
이러한 보안상의 취약점을 해결하기 위해 등장한 것이 **HTTPS**입니다.

- **HTTPS**(HyperText Transfer Protocol Secure): HTTP 프로토콜에 데이터 암호화 계층(SSL/TLS)을 추가한 방식입니다. 
- **보안성**: 모든 데이터를 암호화하여 전송하므로 중간에 데이터를 탈취당하더라도 내용을 확인할 수 없어 안전한 정보 전달이 가능합니다.
